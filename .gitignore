# dataset — too large for git, download from Kaggle
data/creditcard.csv

# dataset — not in git, generate locally
# model artifact is committed so Render can use it without a build-time training step

# env
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# OS
.DS_Store
